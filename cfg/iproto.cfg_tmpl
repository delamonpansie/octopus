#######################################################################
# IPROTO
#
# Primary port (where updates are accepted)
primary_addr="ANY", ro
primary_port=0, ro

# Secondary port (where only selects are accepted)
secondary_addr="ANY", ro
secondary_port=0, ro

# warn about requests which take longer to process
too_long_threshold=0.5
