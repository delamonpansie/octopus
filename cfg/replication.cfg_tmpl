#######################################################################
# LogIO Replication, µSharding, PAXOS

#

# never ever change hostname
hostname = NULL, ro
peer = [
  {
     name = NULL, required
     addr = NULL, required
     replication_port = -1
  }
], ro
